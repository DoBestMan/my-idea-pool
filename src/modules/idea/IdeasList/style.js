import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(theme => ({
  tableRow: {
    '&:hover .makeStyles-actions-26': {
      display: 'flex',
      justifyContent: 'space-between',
    },
  },
  actions: {
    display: 'none',
  },
}));
